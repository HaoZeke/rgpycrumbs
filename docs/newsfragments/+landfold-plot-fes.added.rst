``rgpycrumbs landfold plot-fes`` draws a ``landfold fes --csv`` grid through
the NEB GP landscape (``chemparseplot.plot.landfold`` /
``rgpycrumbs.surfaces``, default ``grad_imq``). Landfold owns the invert;
this command only dispatches the figure.
