eOn job-config tooling uses ``eon-schema>=0.2`` (``write_eon_config``,
``seed_dimers``, MLflow hydrate) instead of eon-akmc ``ConfigClass``.
Install with ``pip install 'rgpycrumbs[eon]'``. CLI no longer treats
site-packages wheels as monorepo editables when resolving ``uv`` peers.
